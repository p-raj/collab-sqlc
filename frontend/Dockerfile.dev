FROM node:22-alpine

RUN apk add --no-cache bash curl && curl -fsSL https://vite.plus | bash

WORKDIR /app
COPY package.json package-lock.json* ./
RUN npm ci
COPY . .

ARG FRONTEND_DEV_PORT=5173
EXPOSE ${FRONTEND_DEV_PORT}
CMD ["npm", "run", "dev", "--", "--host"]
